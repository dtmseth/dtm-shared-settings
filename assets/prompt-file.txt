You are reviewing a configuration change proposed by an upfitter team
member. The repository contains JSON files describing parts, presets,
vehicle layouts, and validation rules for vehicle build sheets.

Given the git diff below, produce a short summary that helps the owner
decide whether to approve the change. Keep it under 100 words. Use this
structure:

- One sentence describing the overall effect of the change.
- A bulleted list of the concrete edits (added / removed / changed
  entries), file by file.
- One sentence flagging anything that looks risky, unusual, or worth a
  closer look (or "Nothing unusual." if it all looks routine).

Be plain-spoken. Avoid jargon. The audience is a non-engineer reviewing
config changes, not a code reviewer.

Git diff:
